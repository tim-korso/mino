# 2026-06-18

## Session log

### 门禁 B 落地 — 两击规则从 prompt 升级到 hook (00:40)

CC 完成功劳行为模式门禁 B 的概念验证 + 部署。里程碑事件：prompt 级约束首次升级到 hook 级硬拦截。

**已部署**：
- `~/.claude/hooks/two-strike.sh` — PreToolUse hook（Bash/Edit/Write/MultiEdit/NotebookEdit），三级拦截 BLOCK→LOCK→LOCKOUT
- `~/.claude/hooks/gate-b-lookup.sh` — 沉积数据喂回查询（--recent / --hot / <tool> <path>）
- `~/.claude/settings.json` — SessionStart hook 注册（`gate-b-lookup.sh --hot 10`，每次新 session 启动自动查最近失败模式）
- settings.json chmod 444 + chflags uchg（防 #32376 自我改写）
- op_key 归并：Write/Edit/MultiEdit 只按路径归一化，不含内容指纹

**验证通过**：
- 喂回链路：gate-b-lookup 精确查询 pattern_id ✅
- 替代路径检测（echo>/cat>绕过 Write hook）✅
- fail-open + 可见日志（ERR trap 时 allow）✅
- LOCKOUT 是 session 级（不永久锁死，给 agent 换思路的机会）✅

**RFC 背书**：GitHub #45427 "Deterministic tool gate"（Dimitri Geelen，545+ 任务、12 个月生产数据）
- 核心原则：**拦截面 = 数据采集面** — 门禁不是评分系统，是沉积数据的采集机制
- 三层记忆映射：Working Memory→门禁A预检 / Project Memory→门禁C沉积 / Episodic Memory→04-MEMORY

**娜娜对齐讨论要点**：
- 门禁 B 数据质量最高（行为数据 vs 自述数据）
- 门禁 C 作弊风险被沉积喂回自消解（假数据→假 Project Memory→伤害 agent 自身）
- 过渡期（2-4 周积累阶段）最脆弱——有门禁但门禁不够聪明
- 优先顺序维持：B→A→C，但动机从"最简单"变为"数据质量最高"

### 04-MEMORY 规则分级 (00:45)

标注哪些 P0 规则已有 hook 级兜底，哪些仍是 prompt 级：

**Hook 级（已落地，不再靠自觉）**：
- D-02 两击规则 → PreToolUse hook `two-strike.sh`
- D-05 引用 >24h 前结果 → 待门禁 A 预检覆盖

**Prompt 级（仍有，需要自觉）**：
- D-01 改关键配置未先备份 → 仍靠 PreToolUse backup hook
- D-03 部署前未构建验收 → 仍靠 Stop verify hook
- D-04 工具调用 error 未报告 → 仍靠 Stop integrity hook

**边界模糊（hook 兜底不完全）**：
- D-08 同路径连续失败 → hook 有 LOCKOUT 但 session 级重置 + 误杀风险
- D-13 前置 Agent 失败后后续编造数据 → 靠 Scout/Judge 分离但不是 hook 级

### 04-MEMORY 更新 (00:50)

同步门禁 B 落地 → 04-MEMORY。标注规则分级。更新 Ongoing Context。

