# 2026-06-08 日志

## Bridge Monitor 误报根因 + Commander 处置

- **Bridge Monitor cron 执行**：Mino Bot 判定 DEGRADED (plugin-not-ready)，AICode Bot 保持 DEGRADED (bridge-unreachable)，Mino 状态变更已告警 Commander
- **根因发现 (Commander)**：MyAgents 内置 bridge monitor 硬编码 `/status` 探测，但 agent-sidecar bridge (`server-dist.js`) 只实现 `/health`。`/status` 返回 404 被误判为 DEGRADED
- **当前真实状态**：两个 bridge 进程均已退出 — Mino (PID 66043), AICode (PID 25226) — 都是 DEAD
- **Commander 已部署修复**：
  - Bridge-Health-Fixer cron (每 5 分钟)：用 `/health` 实测修正 heartbeat，覆盖 MyAgents 的错误 DEGRADED 写入
  - Commander-HealthCheck cron v2 (每 30 分钟)：统一 agent-sidecar 用 `/health`
- **Commander session 查找方式变更**：cron 任务中硬编码的 session ID (58bcaaba-...) 已过期，需改用 `register_session.py lookup commander` 动态获取（当前: 381396f3-...）

## 教训

- Bridge Monitor cron 任务的 session ID 不能硬编码——session 重建后 ID 会变。应改为动态 lookup
- `/status` vs `/health` 端点不匹配 = Bridge Monitor 的根本性缺陷，不仅是这次的问题
